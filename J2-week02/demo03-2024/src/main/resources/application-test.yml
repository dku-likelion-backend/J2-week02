spring:
  datasource:
    url: jdbc:h2:mem:db_test;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
custom:
  secret:
    jwt:
      secretKey: "local-demo03-test-secret-key"
